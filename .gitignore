.DS_Store
__pycache__/
*.pyc
.ipynb_checkpoints/
artifacts/
*.pptx
.antigravityignore
kb/.DS_Store

# Agent-generated (do not commit; Cursor / local agent may create these)
SKILL.md
**/SKILL.md
# ...except the deployed skill manifest, mirrored here for parity
!skill/SKILL.md

# Local-only docs / scratch (optional)
UX_rule.md
/skills.md
commit_policy.md
se_evaluation_framework.md
PLAN_*.md
*-export-*.json

# Secrets + local Langfuse credentials (never commit)
.cursor/
.env
.env.*
.env.example

# Usage telemetry + analytics tooling (local only — do not push)
kb/analytics/
local/scripts/usage_analytics.py
analyze_kb_analytics.py

# Success story source PDFs (local only — markdown lives in kb/case-studies/)
Success Story Library*.pdf
_Master Success Stories*.pdf
API+DOCUMENTATION.pdf
Enterprise Messaging APIs for WhatsApp Business.pdf

github_kb_*.py
kb_answer_runtime.py
Regression Scripts/
patch instructions/
tools/

# YouTube auth + transcript build intermediates (never commit; secrets/raw)
cookies.txt
**/cookies.txt
local/raw/probe_dir/
local/raw/ytsub/
local/raw/*.json3
local/raw/*.srt
local/raw/*.vtt
local/raw/video_meta.json

# User email PII reports (local only — do not push)
local/reports/top_users_gupshup_knowlarity.csv
