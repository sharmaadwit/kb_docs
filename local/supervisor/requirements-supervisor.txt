# KB Supervisor Agent Dependencies

# HTTP client for Qwen proxy (Bearer auth, OpenAI-compatible API)
requests==2.31.0

# Langfuse SDK for trace fetching (delta updates from last cached timestamp)
langfuse==2.1.0

# Environment variable loading (.env file with override support)
python-dotenv==1.0.0

# CLI framework (argparse, structured output)
click==8.1.3

# Data validation and settings management (Pydantic v2 with fallback to v1)
pydantic==2.0.0
